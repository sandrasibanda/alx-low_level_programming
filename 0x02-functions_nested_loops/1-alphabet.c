#include <stdio.h>
#include "main.h"

/**
 * print_alphabet - prints the alphabet in lowercase, followed by a new line.
 *  * Return: Always 0 (Success.)
 */
int main(void)
{
	char ch;

	ch = 'a';
	while (ch <= 'z')
	{
		printf("ch");
		ch++;
	}
	printf("\n");
	
	return 0;
}
