#include <stdio.h> 
#include "main.h"


/**
 * puts_half - prints the second half of the string
 * @str: pointer to a string
 * Return: 0
 */

int main(void)
{
    char *str;

    str = "0123456789";
    puts_half(str);
    return (0);
}
